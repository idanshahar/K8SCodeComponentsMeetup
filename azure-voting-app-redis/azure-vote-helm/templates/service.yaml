apiVersion: v1
kind: Service
metadata:
  name: {{ template "azure-vote-helm.fullname" . }}
  labels:
    app: {{ template "azure-vote-helm.name" . }}
    chart: {{ template "azure-vote-helm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "azure-vote-helm.name" . }}
    release: {{ .Release.Name }}
